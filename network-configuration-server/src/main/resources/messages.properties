network.name.empty = Network must have a name.
network.layers.null = Network must have layers.
network.learningRate.leZero = Network's learning rate must be greater than 0.
network.batchSize.leZero = Network's batch size must be greater than 0.
network.nInputs.leZero = Network's inputs count must be greater than 0.
network.nOutputs.leZero = Network's outputs count must be greater than 0.
network.layers.empty = Network must have layers.
network.layers.invalidLinkConfig = Network's layer config is not correct.
layer.type.empty = Layer must have a type.
layer.activation.empty = Layer must have an activation type.
layer.nodes.null = Layer must have nodes. 
layer.nInputs.ltZero = Layer's inputs count must be greater or equal to 0.
layer.nNodes.leZero = Layer's nodes count must be greater than 0.
layer.nOutputs.ltZero = Layer's outputs count must be greater or equal to 0.


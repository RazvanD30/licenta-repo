spring.application.name=config
# encrypt.fail-on-error=false
# encrypt.key-store.location=classpath:keystore-test.jks
# encrypt.key-store.password=${KEYSTORE_PASSWORD:test}
# encrypt.key-store.alias=test

# program arguments --key-store-path=keystore-test --key-store-password=12345678
# keytool -keystore keystore-test -genkey -alias test -storetype jceks


# https://support.globalsign.com/customer/en/portal/articles/2121490-java-keytool---create-keystore